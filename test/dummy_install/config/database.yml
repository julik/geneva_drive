# PostgreSQL configuration for GenevaDrive installation test
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: geneva_drive_install_development

test:
  <<: *default
  database: geneva_drive_install_test
